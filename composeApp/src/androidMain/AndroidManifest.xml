<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".HomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".dnd.spellbook.SpellBookActivity"
            android:theme="@style/NoActionBar" />

        <activity
            android:name=".dnd.spellbook.SpellDetailActivity"
            android:theme="@style/NoActionBar" />

        <activity
            android:name=".dnd.inventory.InventoryActivity"
            android:theme="@style/NoActionBar" />

        <activity android:name=".xml.game.GameListLegayActivity" />
        <activity android:name=".xml.group.CharacterListLegacyActivity" />

        <activity
            android:name=".xml.spellbook.SpellBookLegacyActivity"
            android:theme="@style/NoActionBar" />
        <activity
            android:name=".xml.bestiary.BestiaryLegacyActivity"
            android:theme="@style/NoActionBar" />
        <activity
            android:name=".xml.inventory.InventoryLegacyActivity"
            android:theme="@style/NoActionBar" />

    </application>
</manifest>
